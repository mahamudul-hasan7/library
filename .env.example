# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=brainroot_library

# Environment
ENVIRONMENT=development
# For production: ENVIRONMENT=production

# API Configuration (add if needed)
API_URL=http://localhost:8000
